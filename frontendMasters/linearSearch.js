function linearSearch(list, item) {
  let index = -1;
  list.forEach((listItem, index) => {
    if (listItem === item) {
      index = i;
    }
  });
  return index;
}

linearSearch([2, 6, 7, 90, 103], 6);
